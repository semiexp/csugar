from setuptools import setup, find_packages


setup(
    name='pycsugar',
    version='0.0.1',
    packages=[''],
    package_dir={'': 'build'},
    package_data={'': ['pycsugar.cpython-38-x86_64-linux-gnu.so']}
)
